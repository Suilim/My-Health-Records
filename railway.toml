[deploy]
startCommand = "mkdir -p .streamlit && echo \"$STREAMLIT_SECRETS\" > .streamlit/secrets.toml && echo '---secrets.toml content---' && cat .streamlit/secrets.toml && streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
