[deploy]
startCommand = "mkdir -p .streamlit && python3 -c \"import os; open('.streamlit/secrets.toml','w').write(os.environ['STREAMLIT_SECRETS'])\" && streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
